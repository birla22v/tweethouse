<%= form_for [current_user, @shout] do |f| %>

  <div class="field">
    <%= f.label 'shout' %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">	
    <%= f.submit %>
  </div>
<% end %>

